# Install environment:
#   $ conda env create --file environment.yml

name: my-project

dependencies:
  # change the version here if you need to
  - python=3.8
  # add dependencies here
  - numpy
  - pandas
  - matplotlib
  - pip
  # if you need to install something using pip, list it here
  - pip:
    - ploomber
